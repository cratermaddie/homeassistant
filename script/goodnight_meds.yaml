goodnight_meds:
  sequence:
    - condition: template
      value_template: >-
        {{ is_state('input_boolean.meds_evening', 'off') }}
    - service: tts.cloud_say
      entity_id: media_player.bedroom_speaker
      data_template:
        message: "You still need to take your meds."